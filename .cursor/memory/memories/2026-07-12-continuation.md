# 2026-07-12 continuation

- **Social radar stuck on "Scanning social radar…"** — root cause: dev auth bypass started with `loading: false`, venue fetch ran before stale JWT cleared from AsyncStorage; Supabase JS client hung on venue read.
- **Fix (v2):** Replaced `lib/supabasePublic.ts` raw `fetch` + `AbortController` (caused "fetch request has been cancelled" in RN) with `lib/supabaseAnon.ts` — dedicated Supabase client with `persistSession: false`; retry on transient cancel/network errors.
- **Verified:** curl anon REST returns venues (The Ivy, Oxford Art Factory, The Beresford); `tsc --noEmit` pass.
- **User action:** reload simulator (Cmd+R) or restart `npx expo start --ios --port 8081 --clear`.
- **v5 social UI (2026-07-12):** Added `design/ui/sidequest-social-v5-inbox-activity-discovery.png`; unified theme to icon purple `#371259`; new main tab bar + screens — `app/(main)/(tabs)/{inbox,activity,map,discovery,connections}.tsx`; shared `FilterPills`, `InboxThreadRow`, `ActivityFeedRow`, `DiscoveryProfileCard`; mock feed `lib/socialMock.ts`; post-check-in default route → Inbox. `tsc --noEmit` pass.
- **Mode + visibility (2026-07-12):** Same-mode-only discovery (`lib/peerMode.ts`, `loadRoomData(venueId, viewerMode)`); `ModePhotoTag` on checked-in avatars; visibility ON by default; mode/visibility in profile; removed coral selection border on `RoomPeerCard`. Device rebuild `Free Malware`.
- **Map UX + keyboard (2026-07-12):** `KeyboardDismissView` at app root; map filters + venue rail below search; keyboard dismiss on map pan. Rebuilt `Free Malware`.
- **Social v5 complete (2026-07-12):** Wave animation + inbox thread (`lib/waves.ts`); Activity local feed; Discovery/Connections wave FABs; `Same Mode Only` profile toggle; venue presence tag on hero; map profile FAB bottom-left; Sign Out on profile only; `loadProfileFormFields` fixes check-in profile error. Rebuilt `Free Malware`.
- **App icon full-bleed (2026-07-12):** `scripts/generate-app-icons.py` now crops opaque raised-button art from master and scales to 1024 edge-to-edge (3D border at icon bounds, `#371259` corners). Regenerated `assets/images/*` + `ios/.../App-Icon-1024x1024@1x.png`. Rebuilt `Free Malware`.
- **UI fixes (2026-07-12):** Map profile FAB → `/(onboarding)/profile` (was blocked by main layout redirect). `KeyboardDismissView` no longer wraps app in `TouchableWithoutFeedback` (venue scroll fix). `Screen` adds safe-area top inset (check-in profile no longer clipped). Who's Here block moved directly under venue hero image. New `AppIcon` component — minimal single-color icons app-wide (`colors.iconMuted`). Rebuilt `Free Malware`.
- **Check-in fix (2026-07-12):** Mock `DEV_AUTH_BYPASS` without session now saves local check-in (`lib/devLocalCheckIn.ts`); real sessions call `ensureProfile` + upsert before insert. Required field `*` + disabled-button hint on check-in screen. Rebuilt `Free Malware`.
- **Tab bar + room roster (2026-07-12):** Custom `MainTabBar` matches v5 design (purple bar, elevated coral Map FAB). Unified `resolveVenueAttendees` for Who's Here counts + View Room list; removed Jordan testing copy; mode-filtered fake peer; includes self when checked in. Rebuilt `Free Malware`.
- **Social UX pass (2026-07-12):** Glass morphism (~90% opacity) on Card, FilterPills, tab bar, AppHeader. Activity filters `fit` (no horizontal scroll). Map tab renders Social Radar inside tabs (`map.tsx` re-exports venue screen); coral FAB icon-only (no clipped label). `SwipeableRow` delete/archive/report on inbox + activity. New `/(main)/report` + `/(main)/archived`. Jordan-only mock inbox + `DEV_JORDAN_CONNECTION_ID` local dev chat. Edge swipe back/forward via `SwipeNavEdges` + `lib/navHistory.ts`. `tsc --noEmit` pass.
- **Settings/Profile v6 (2026-07-12):** Redesigned `UserProfileScreen` to match `design/ui/sidequest-settings-profile-v6-reference.png` — hero card with avatar + status dot, settings menu (Account, Privacy, Notifications, Vibe Tags, Saved Locations), Log Out / Delete Account pills, account status footer. New `SettingsMenuRow` + AppIcon lock/bell/tag/bookmark. Standing rule: auto-copy design images to `design/ui/`.
- **Device rebuild (2026-07-12):** Full rebuild to **Free Malware** (`npx expo run:ios --device "Free Malware"`) — build succeeded, app installed.
- **Fix pass (2026-07-12):** Removed glass/transparency app-wide (solid `colors.card`/`colors.tabBar`). Fixed venue tap → inbox bug (checked-in users now route to `/(main)/venue/[venueId]`). Bottom menu per v5: Home, Discover, Map (coral pin FAB), Alerts, Profile tab. Rebuilt Free Malware.
- **Tab/nav v2 (2026-07-12):** Tabs → Home (discovery deck), Alerts, Map (dominant coral pin FAB w/ purple ring), Check-ins, Profile. Room = former Discovery at `/(main)/room`; View Room opens it. SF Symbols: house, bell, mappin, building.2, person. Rebuilt Free Malware.
- **Forward swipe + solo check-in (2026-07-12):** Fixed forward nav — `recordBackFrom`/`consumeForward` in `lib/navHistory.ts`, full-screen right→left forward in `SwipeNavEdges`. Solo check-in never gated on peers: `venueRoomRoute` routes pre-check-in to onboarding room (was bouncing to main); `appendSelfIfNeeded` always includes self; positive presence copy in `lib/venuePresence.ts`; room empty states + solo banner on `RoomFeedScreen`. Standing rule in `USER.md`. Rebuilt Free Malware.
- **Home/room unification (2026-07-12):** View Room = same `RoomFeedScreen` as Home tab (`venueRoomRoute` → `/(main)/(tabs)/home` when checked in; onboarding room re-exports `RoomFeedScreen`). `CheckInStatusTag` (green checked in / coral not). `WaveButton` green pill with wiggle animation; removed profile button — tap photo/name opens public profile. Rebuilt Free Malware.
- **Semantic colors + notify timers (2026-07-12):** `lib/semanticColors.ts` + theme tokens. Alerts rows color-coded by type; Check-ins active (green) vs previous (muted); inbox requests pink. `WaveButton` 3D green → yellow 30s `NotifyTimerBar` → red Un-Wave; `unwaveUser`. Chat first message same 30s delay via `useNotifyTimer`. Past check-ins on Check-ins tab (`lib/checkinHistory.ts`). Rebuilt Free Malware.
- **Route restructure (2026-07-12):** Removed `(auth)`, `(main)`, `(onboarding)`, `(tabs)` folders → `app/auth/`, `app/main/tabs/`, `app/onboarding/`. All hrefs updated; `lib/routes.ts`. Chat `headerBackTitle: 'Inbox'`. Un-Wave uses `waveOff` icon only. Rebuilt Free Malware.
- **Mode colors + profiles (2026-07-12):** `MODE_PALETTE` in `lib/semanticColors.ts` — friends purple, dating pink, networking blue (`#2563EB`). `CheckInStatusTag` tag + dropdown use mode palette; closes on Home feed scroll. `ModePhotoTag`/`ModeCard`/peer profile mode label aligned. `lib/publicProfile.ts` resolves all `MOCK_DISCOVERY_PROFILES` + `mock-kal`; `app/main/peer/[userId].tsx` + `navigateToPublicProfile` so profiles open when checked in. Updated Home, Activity, Inbox, Archived, Connections. `tsc --noEmit` pass.
- **Dark mode + tab bar + inbox routing (2026-07-12):** Default dark theme (`contexts/ThemeContext.tsx`, `darkColors`/`lightColors`); Light mode toggle in Profile → Settings. Tab bar tint follows check-in mode (purple/blue/pink). Alerts requests → profile + green Message/Reply; Inbox always opens chat (`lib/inboxNavigation.ts`). Rebuilt Free Malware.
- **Profile inbox CTA everywhere (2026-07-12):** Green Message/Reply on peer profiles whenever user has inbox or alerts outreach (`shouldShowProfileInboxAction` in `lib/inboxAction.ts`); loads inbox + activity on every profile open. Mock Kal inbox thread added. Works from Home, Alerts, Connections — not only `fromAlert=request`.
- **Contrast + home gate + map checkout (2026-07-12):** Dark theme text/icons — white on dark surfaces, no purple-on-black; theme tokens + DiscoveryProfileCard/InboxThreadRow/ui outline fixes. Home hides profiles when not checked in; “Check in now” → `/main/tabs/map`. Checkout/leave → `mapTabRoute()` (main map tab); main layout no longer requires check-in. Rebuilt Free Malware.
- **Mode-driven app primary (2026-07-12):** Purple is friends-mode primary only; active Open To mode tints app-wide via `lib/modeTheme.ts` + `ThemeContext.setActiveMode` + `ModeThemeBridge`. Updated MainTabBar, map pins/filters, chat bubbles/send, CheckedInStrip, TooltipOverlay, onboarding mode/check-in. Activity semantic colors unchanged. `tsc --noEmit` pass; rebuilt Free Malware.
- **Solo check-in + cross-mode room copy (2026-07-12):** Removed visibility gate blocking check-in on mode screen. `loadRoomData` returns presence counts; no mock-profile fallback when live + empty room. `presenceInMyModeLabel` + `roomFeedEmptyMessage` distinguish empty venue vs others in different modes. Venue Who's Here + Home empty states updated. Rebuilt Free Malware.
- **Ivy test peers per mode (2026-07-12):** `lib/devFakePeer.ts` — Jordan (friends), Alex (networking), Sania (dating) at The Ivy; all three injected into venue roster; room feed shows mode-matching peer. Profiles in `devPeerProfile.ts`. Rebuilt Free Malware.
- **Room feed + tab/map UI (2026-07-12):** `loadRoomData` now derives peers from `resolveVenueAttendees` (same source as counts) so mode switch shows Ivy test user; removed tab bar shadow/white map FAB border; removed map profile FAB. Rebuilt Free Malware.
- **Test waves + inbox messages (2026-07-12):** Expanded `MOCK_ACTIVITY_ITEMS` (waves from Jordan, Alex, Sania, Mia, Liam) and `MOCK_INBOX_THREADS` (message threads + wave request); seeded inbound chats in `devPendingChat` + extra Jordan messages in `devJordanChat`.
- **Mode counts + map eligible + profile fix (2026-07-12):** `CheckInStatusTag` shows per-mode counts e.g. Friends (2) in collapsed chip and dropdown rows; map pins/rail cards use coral “Check in” styling for venues within check-in range; fixed `UserProfileScreen` hooks violation (`useMemo` after early return). Rebuilt Free Malware.
- **Attendee counts exclude viewer (2026-07-12):** All presence/mode/map counters exclude the signed-in user when checked in at that venue (`lib/venuePresence.ts` `viewerExclusionUserId`); peers list unchanged. Rebuilt Free Malware.
- **Background notify timers (2026-07-12):** `lib/notifyTimerService.ts` — wave/chat 30s timers survive navigation and auto-send on expiry; `NotifyTimerBar` adds **Send Now** + **Stop**. Rebuilt Free Malware.
- **Mode-primary headers (2026-07-12):** Chat stack header + `AppHeader` `primary` variant use active Open To accent (not white); Inbox/Archived/Report updated. `modeStackHeaderOptions` in `lib/modeTheme.ts`. Rebuilt Free Malware.
- **Tab bar corner fill (2026-07-12):** `MainTabBar` outer shell uses `colors.background` so rounded top corners show app bg (not white); iOS continuous curve + subtle bevel edge; tabs `tabBarStyle` synced. Rebuilt Free Malware.
- **Map tab bar + check-in gate (2026-07-12):** Floating transparent tab bar on map (map visible in bevel ears); `useCheckInGate` blocks wave/message when not checked in with alert + Go to Map. Rebuilt Free Malware.
- **Home checkout button (2026-07-12):** `RoomFeedScreen` shows outline **Check out** below status tag on Home tab when checked in; confirms then `performCheckout`. Rebuilt Free Malware.
