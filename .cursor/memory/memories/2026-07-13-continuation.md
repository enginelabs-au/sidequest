# 2026-07-13 continuation

- **Android Google OAuth complete:** Added `EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=300778226594-508mlkmjir3flai0ppnotbj4niu81paf.apps.googleusercontent.com` to `.env` / `.env.example`. `app.config.ts` exposes `googleAndroidClientId`. `lib/auth.ts` validates Android client on Android + clearer Supabase audience error. `lib/googleAuthConfig.ts` helper for authorized ID list. Scripts/docs updated (`patch-supabase-native-auth.sh` uses `external_google_client_id` with Web+iOS+Android comma-separated; fixed wrong `external_google_authorized_client_ids` field). Supabase remote verified: 3 authorized Google client IDs. `npm run test:oauth` pass. **User:** `npx expo prebuild --clean --platform android && npx expo run:android` to test native Google sign-in on device/emulator.
